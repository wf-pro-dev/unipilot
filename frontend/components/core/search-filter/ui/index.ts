/**
 * Shared UI components for search and filtering
 * 
 * These components are pure presentation - no business logic.
 * Used by both SearchFilter (client-side) and SearchFilterServer (server-side).
 */

export { SearchInput, type SearchInputProps } from './search-input'
export { FilterDropdown, type FilterDropdownProps } from './filter-dropdown'
export { FilterBar, type FilterBarProps } from './filter-bar'