{
  "name": "unipilot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wails dev",
    "build": "echo Build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {}
}
